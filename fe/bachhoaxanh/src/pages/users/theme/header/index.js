
import {memo } from "react";
import "./style.scss"
 const  Header = () => {
    return (
    <div className="heard__top">
        <div className="container">Header</div>
    </div>
    );
    
};

export default memo(Header);